# Your TypeSafe AI key. Deliberately NOT prefixed with VITE_ so it never
# reaches the browser bundle -- the dev server proxies calls for you.
JEV_API_KEY=sk-ts-...
# jev-latest | jev-preview | jev-1.13.0
JEV_MODEL=jev-latest
